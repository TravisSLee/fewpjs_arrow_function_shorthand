let divide = function() {
    let product = 2000 / 100
    return product
}

let square = (num) => {
    let product = num * num
    return product
}

let add = (num1, num2) => {
    let sum = num1 + num2
    return sum
}